---
// Markdown 渲染组件，接收字符串并渲染为 HTML
import { marked } from 'marked';

interface Props {
  content: string;
}

const { content } = Astro.props;
const html = marked.parse(content);
---

<div class="prose dark:prose-invert max-w-none" set:html={html} />
